apply plugin: 'java'

sourceCompatibility = 1.5
version = '0.1.0-SNAPSHOT'

defaultTasks 'test'

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.+'
  testCompile group: 'org.jmock', name: 'jmock-junit4', version: '2.6+'
}

repositories {
  mavenLocal()
  mavenCentral()
}
